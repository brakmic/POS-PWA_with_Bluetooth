import { ValidateFunction } from 'ajv';
import { RequestHandler } from 'express';
import { OpenAPIV3 } from 'openapi-types';
export declare const middleware: (operationId: string, operations: Map<string, ValidateFunction>, exclude: boolean, operationObject?: OpenAPIV3.OperationObject) => RequestHandler;
